.input-field
  = mtl_label @f, :name
  = text_input @f, :name
  = error_tag @f, :name

.input-field
  = mtl_label @f, :description
  = textarea @f, :description, rows: 3, class: "materialize-textarea"
  = error_tag @f, :description
